# Quality Tracker

The single source of truth for open quality items in InterchangeDB. Items
carry `Q-NN` numbers (the older `ISS-NNN` scheme was folded into this
one). Resolved items are intentionally omitted from the tables. Closed
items live only in the audit history at the bottom.

The tracker opened with the May 24, 2026 audit (commit `0aaff20`), which
found 22 items — five of them `plan.md` checkboxes that overstated the
code. All were closed except the meta-item below. The lesson stands:
checkboxes drift silently between intent and record.

---

## Open quality items

Severity: Critical / High / Medium / Low.
Effort: S (hours), M (1-2 days), L (several days), XL (week+).

| #    | Item                                                                                                                                                      | Sev  | Effort | Status |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ------ | ------ |
| Q-23 | Plan-vs-code checkbox audit. Walk every `[x]` in `plan.md`, verify against code, downgrade overstated ones. Cost: hours. Value: removes credibility risk. | High | M      | Open   |

Future-work candidates are not tracked here until adopted. They live
with their subject: swappable-seam extractions in
[`docs/seams.md`](docs/seams.md), testing extensions in
[`docs/stability.md`](docs/stability.md), and TPC-H levers in
[`docs/plan-tpch.md`](docs/plan-tpch.md).

---

## How to use this document

- A new quality concern gets the next free `Q-NN` number. Preserve
  numbering when items close — historical references must always
  resolve.
- When you close an item, move it out of the table and append a one-line
  entry to the audit history at the bottom, with the closing commit.
- The old `ISS-NNN` IDs are referenced parenthetically inside the
  relevant `Q-NN` rows for historical traceability.

---

## Audit history

| Date       | Commit    | Notes                                                                                                                                                      |
| ---------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-24 | `0aaff20` | Initial combined audit. 22 items open. 5 plan-drift findings. ISS-001 (page checksum) and ISS-002 (LSM mutex poisoning) closed prior; not carried forward. |
| 2026-05-27 | `0aaff20` | Q-23 sweep added Q-26 (`gc_status` missing observability hook). Phase 6/7 plan claims for `mvcc_put`/`mvcc_delete` and `register_snapshot`/`unregister_snapshot` annotated as NOTE-refactors in `plan.md` (capability exists under different names/structure: `TxnEngine`, `TransactionManager::active_read_timestamps`). |
| 2026-05-27 | (uncommitted) | Q-21 closed: `render_explain` nested arm now wraps inner text with `"Explain\n"` + 2-space indent per line. Regression test `plans_nested_explain_preserves_inner_render` hand-builds nested `LogicalPlan::Explain` (parser rejects nested EXPLAIN syntactically, so SQL surface is unaffected). |
| 2026-05-27 | (uncommitted) | Q-02 closed: `tests/common/{golden_plan,mock_catalog,mock_stats}.rs` + `mod.rs` + `tests/common_smoke.rs` smoke binary. 18 inline tests cover the scaffolding. Design deviations from the plan: (a) `MockCatalog` wraps the real `Catalog<BTreeEngine>` via tempdir rather than introducing a `CatalogRead` trait — avoids refactoring binder/planner/executor signatures right before Phase 12; (b) `MockStats` is a plain struct, not a trait impl — Phase 14 will add the `StatsProvider` trait once the cost model determines its shape; (c) histograms deferred to Phase 14 per same rationale. |
| 2026-05-27 | (uncommitted) | Q-11 closed: `tests/explain_goldens.rs` — 17 goldens covering CREATE TABLE, BEGIN/COMMIT/ROLLBACK, every SELECT shape (bare/projection/filter/limit/multi-col), INSERT (single + multi-row), UPDATE (with/without filter), DELETE (with/without filter), EXPLAIN (wrapping each). Failures pinpoint plan structure changes before Phase 14 starts rewriting. |
| 2026-05-27 | (uncommitted) | Q-04 closed: SSTable writes now go to `<id>.sst.tmp`, fsync, rename to final `<id>.sst`, plus best-effort dir fsync. Manifest `log_add`/`log_del` add `sync_all` after each entry. `Manifest::open` sweeps orphan `*.sst.tmp` files. 6 regression tests in `tests/lsm_atomic_flush_test.rs` cover orphan sweep, durability across drop+reopen, malformed-tmp isolation, repeated-flush leak check. All 583 lib + 14 LSM-adjacent integration tests pass. |
| 2026-05-27 | (uncommitted) | Q-05 closed: `BTreeScanIterator::load_next_page` validates decoded leaf invariants (`keys.len() == values.len()`, tombstone indices in range) and bounds-checks `next_page_id` against `bpm.disk_page_count()` before assigning it. Validation fires before yielding any entries from the current page — defensive, since a corrupt sibling pointer may signal broader corruption. 3 regression tests in `tests/btree_iter_bounds_test.rs`: out-of-range sibling triggers `StorageCorrupted`, `PageId::INVALID` terminator still works, boundary value (`next_page_id == page_count`) is rejected. |
| 2026-05-27 | (uncommitted) | Q-06 closed: `pub trait DiskManager` extracted with 5 methods (`read_page`, `write_page`, `allocate_page`, `page_count`, `file_size`). Old `DiskManager` concrete struct renamed to `FileDiskManager` (unchanged behavior). New `MemoryDiskManager` stores pages as raw `[u8; PAGE_SIZE]` arrays (avoids weakening Page's `#[cfg(test)] Clone`). `BufferPoolManager::new` is now generic over `D: DiskManager + 'static` and boxes internally — zero call-site changes beyond the rename. Mass rename across 32 files via sed. 589 lib + 17 EXPLAIN goldens + 6 LSM atomic flush + 3 btree iter bounds + 28 database integration all pass. |
| 2026-05-27 | (uncommitted) | Q-07 closed: `tests/common/fault_injection.rs` ships `FaultInjectionDiskManager<D>` wrapping any `DiskManager` with `AtomicUsize` counters for read/write/allocate failure injection. Pre-configured before handoff to BPM. 5 unit tests (counter consumption, independence per op-kind, pass-through). `tests/fault_injection_test.rs` adds 5 BPM-level integration tests — proves BPM propagates `Error::Io` rather than silencing dirty-eviction write failures or flush failures. **Scope deviation:** WAL/SSTable I/O uses raw `std::fs::File` directly, not `DiskManager`, so fault injection there requires separate trait extraction. Documented as deferred in the test-file module comment. |
| 2026-05-27 | (uncommitted) | Q-01 + Q-27 closed together. **Q-27 root cause**: a two-layer race in BPM eviction. (a) `handle_cache_hit` pins *after* releasing pt.read, so concurrent eviction can replace frame contents between lookup and pin. (b) `swap_policy` re-registers frames in the new replacer using a stale `pt` snapshot — if it runs between `evict_for_page` (which removed the victim from the old policy's evictable set) and `pt.remove`, the new replacer marks the victim evictable again, letting a second eviction pick it. **Fix**: `evict_page` now holds `pt.write` across `evict_for_page` (pin-count check is now under the same lock that cache-hit threads block on via `pt.read`) and `swap_policy` holds `pt.write` for its entire duration. Both paths serialize at `pt.write`. Tests: 3 active in `tests/policy_swap_stress_test.rs` — marquee storm + isolation + warm-swap. All 583+ lib, 5 concurrency_stress, 7 BPM, 6 btree_concurrent, 1 deadlock_stress, 27 fault_injection, 3 policy_swap pass. This is the same class of bug that defeated the user's BusTub Project 2. |
| 2026-05-27 | (uncommitted) | Q-24 closed: `tests/hermitage_si_test.rs` — 6 tests covering Adya G0/G1a/G1b/G1c, G2-item (OTV), and G2 write skew. First five are PREVENTED under SI; write skew is documented as ALLOWED (would require SSI to prevent — flagged for future work). G0 test accepts either `WriteConflict` (MVCC layer) or `LockTimeout` (2PL fallback) — both correctly prevent dirty writes. |
| 2026-05-27 | (uncommitted) | Q-26 closed: `GcStatus` struct in `src/txn/gc.rs` exposes `low_water_mark`, `oldest_active_read_ts`, `active_snapshot_count`, `committed_txns_tracked`, `aborted_txns_tracked`, `current_timestamp`. `Database::gc_status()` reads from `TransactionManager` without an engine scan — O(active txns) + hashmap sizes. 5 tests in `tests/gc_test.rs` cover empty state, single/multiple active snapshots pinning the watermark, commit-advances-watermark, txn-manager-absent error path. |
| 2026-05-27 | (uncommitted) | Q-20 closed: `tests/planner_determinism_test.rs` — 10 tests verifying that the same SQL produces byte-identical `pretty_plan` output across N runs (with fresh `MockCatalog` instances per run, so per-instance counters / pointer identity can't leak in). Covers SELECT (bare/projection/filter+limit), INSERT (multi-row), UPDATE, DELETE, EXPLAIN, DDL, transaction control. Cross-instance determinism caught as a separate test. Prerequisite for Phase 19 plan-store hashing. |
| 2026-05-27 | (uncommitted) | Q-19 closed: `tests/binder_proptest.rs` — 6 proptest cases (256 inputs each). Two panic-safety properties: arbitrary byte strings as SQL never panic the binder (empty catalog + populated catalog). Four structural properties: SELECT * resolves known table, SELECT from unknown table errors, INSERT with arity mismatch errors, CREATE→SELECT roundtrip succeeds. ~37s run time. |
| 2026-05-27 | (uncommitted) | Q-08 closed: 4 new proptest cases appended to `tests/proptest_test.rs`. LSM: matches `HashMap` oracle under random put/delete/get/flush sequences (50 ops, 256 inputs); state survives drop+reopen with mandatory flush before drop. Lock manager: table empty after `release_all` per txn for any sequence of uncontested acquires; S+S on the same key always granted (no false-positive deadlock). Total runtime ~210s (compaction triggered by random workloads). |
| 2026-05-27 | (uncommitted) | Q-09 closed: `tests/cross_engine_differential.rs` — random put/delete/get/scan op sequences run in lockstep against `BTreeEngine` and `LsmEngine` (via the `StorageEngine` trait), with a `BTreeMap` oracle as a third witness. Every observation triple-checks engine-vs-engine and engine-vs-oracle. 256 random inputs + 2 smoke tests. Validates the interchange thesis at the storage layer. Invalid scan ranges (start > end) skipped — undefined behavior. |
| 2026-05-27 | (uncommitted) | Q-25 closed by decision: shuttle adoption deferred to Open-Ended (would require replacing all `parking_lot::{Mutex,RwLock}` with std equivalents — significant perf-impacting refactor). `plan.md` Task 8.6 header updated to "manual-barrier substitute" with the deferral rationale spelled out. Existing `tests/deterministic_test.rs` covers the original task's intent at the cost of non-exhaustiveness. |
| 2026-05-27 | (uncommitted) | Q-16 closed: `tests/near_limit_sizes_test.rs` — 13 boundary tests (B+Tree quarter/half/near-page values, LSM at/under/over u16::MAX, empty values, empty keys, single-byte k/v). **Found a real bug along the way**: `BTreeEngine::put` would panic deep in `encode_leaf_node` ("range end out of slice") for oversized values. Added a precondition guard returning `Error::ValueTooLarge` for (key + value) > PAGE_SIZE − LEAF_HEADER_SIZE − tombstone_space − length_prefixes (= 4043 bytes). Panic → controlled error. |
| 2026-05-27 | (uncommitted) | Q-22 partially closed via proptest extension: added `binder_never_panics_on_sql_keyword_laced_input` to `tests/binder_proptest.rs` — composes random fragments from a SQL-keyword grammar (SELECT/FROM/WHERE/etc., identifiers, literals, operators) and asserts no panic. Higher hit rate on deep binder paths than uniformly random bytes. True `cargo-fuzz` target deferred to Open-Ended (requires nightly Rust). |
| 2026-05-27 | (uncommitted) | Q-17 closed: `tests/large_scale_test.rs` — 4 active tests + 2 ignored (1M-scale). B+Tree 25K bulk-insert+lookup, 25K scan returns sorted, random read pattern under eviction pressure (32-frame pool, 50K keys, 5K random reads). LSM 50K bulk-insert. 1M-scale tests for both engines are `#[ignore]`'d (run with `--include-ignored --release`). Scale numbers adjusted from spec's 100K because debug-build + `FileDiskManager`'s fsync-per-write make 100K integration tests impractically slow; criterion benchmarks are the right place for true throughput numbers. |
| 2026-05-27 | (uncommitted) | Q-15 closed at baseline: audit found all 89 `src/*.rs` files already start with `//!` module-level docstrings. The "ongoing" framing was about content quality, which is open-ended and lower-leverage than the structural items we just closed. Treat any individual module-level docstring rewrite as part of normal touched-file maintenance going forward. |
| 2026-06-10 | (uncommitted) | Q-28 opened+closed: CI automation floor (`docs/stability.md` pillar A, leverage rank #1). Added `rust-toolchain.toml` (pin `1.89.0` + rustfmt/clippy, minimal profile) and `.github/workflows/ci.yml` — 5 parallel jobs on **push + PR**, no cron: `fmt --all --check`, `clippy --all-targets -- -D warnings` (warn→deny in CI only, crate `[lints]` stays `warn`), `cargo test`, `cargo test --release`, `cargo doc --no-deps` under `RUSTDOCFLAGS=-D warnings`; `Swatinem/rust-cache` + `cancel-in-progress`. The nightly `--ignored` row from the pillar-A table was **intentionally not adopted** — per-push only. Three green-baseline prerequisites landed first: (a) whole-repo `cargo fmt` (155 files, mechanical whitespace/wrap, stock rustfmt); (b) clippy 33→0 — mostly mechanical fixes plus 3 `#[allow]`s tagged `CLIPPY-ALLOW(...)` (grep-able) on `too_many_arguments` (`plan_select`, `mvcc_scan`) and `should_implement_trait` (`Cost::add`) where a struct/trait refactor wasn't warranted; (c) rustdoc 14→0 — backticked `[i]`/`[0]`/byte-layout prose and `<row>`/`<stmt>` placeholders across 5 files (`node.rs`, `manifest.rs`, `record.rs`, `error.rs`, `binder.rs`) so rustdoc stops parsing prose as broken links / unclosed HTML. All four gates verified green locally. |
| 2026-06-10 | (uncommitted) | **Q-27 reopened as Q-30.** The Q-28 CI's first `test (release)` run failed on `marquee_hot_swap_storm_all_six_policies_both_modes` — the very test Q-27 was closed against. Local release loop: 3/12 fail (marquee) vs 0/20 (no-swap `isolation_*`), so the BPM data race is swap-path-specific and Q-27's fix is incomplete. Quarantined the marquee test with `#[ignore]` (kept as a `--ignored` repro; no-swap bisection guard stays active) so per-push CI is green and deterministic. Real fix tracked as Q-30, earmarked for a `shuttle` session (pillar C). |
| 2026-06-10 | (uncommitted) | Q-29 partial: pillar B1 fuzzing, stable-Rust half. `tests/decode_fuzz_test.rs` — 10 proptest properties over the five decode-arbitrary-bytes surfaces (`keyenc`, `tuple` `decode`/`decode_column`, `wal::LogRecord::decode`, `SSTableReader::open`, `Manifest::open` which drives the private `replay_line`): panic-freedom on arbitrary bytes + round-trip + keyenc order-preservation. On first run it found **two real malformed-input panics**, both fixed in the same change: (a) `tuple::decode_column` advanced `cursor` past the blob via `value_byte_size`'s unchecked fixed widths, then panicked on `&bytes[cursor..]` — added a bounds guard returning `StorageCorrupted`; (b) `SSTableReader::open` had an `assert!(file_size >= FOOTER_SIZE)` that crashed on a short/torn file — converted to a `StorageCorrupted` return, since `open` is also the recovery path for files of unknown provenance (NOTE: deliberate assert→Err on *external* corruption, distinct from internal-invariant asserts). Survives 4000 cases/property. Saved regression seeds committed. **Remaining under Q-29:** the coverage-guided `cargo-fuzz` scaffold in `fuzz/` (manual/nightly, off CI). |
| 2026-06-10 | (uncommitted) | Q-29 completed: `cargo-fuzz` scaffold landed in `fuzz/` — standalone workspace (invisible to root `build`/`test`/`clippy`/CI; verified via `cargo metadata`), shared `Arbitrary` schema mirror in `fuzz/src/lib.rs`, and 5 libFuzzer targets over the same surfaces as the proptest half (`keyenc_decode`, `tuple_decode`, `wal_record_decode`, `sstable_open`, `manifest_open`) + `README.md` run instructions. Manual/nightly by design (`cargo +nightly fuzz run <t>`), off CI per the per-push-only decision. Verified locally: installed nightly + `cargo-fuzz` 0.13.2, `cargo +nightly fuzz build` compiles all 5, and a 15s smoke of each runs clean. **It found a third bug → Q-31.** |
| 2026-06-10 | (uncommitted) | Q-31 found+fixed: the `cargo-fuzz` `keyenc_decode`/`tuple_decode` targets (which feed the raw `u8` Decimal scale, unlike the proptest generator that clamped it) caught a SQL-reachable panic — `CREATE TABLE t (x DECIMAL(p, s))` with `s`/`p` > 18 was accepted by the binder, then crashed `Decimal::from_i64_with_scale`'s `assert!(scale <= MAX_SCALE)` on the next INSERT/read. Fix: `binder::decimal_args` now validates `1 <= precision <= MAX_PRECISION` and `scale <= precision` (checked on the parser's raw `u64`, before the `as u8` cast, to avoid truncation aliasing like `274 as u8 == 18`), returning `Error::SqlParse`. 2 regression tests in the binder test module (reject `DECIMAL(30,25)`, accept boundary `DECIMAL(18,18)`). The fuzz schema mirror now clamps Decimal precision/scale to the validated range, so the targets test arbitrary *bytes* under a *valid* schema — the real trust boundary (a corrupt on-disk catalog crashing via the assert remains acceptable per "crash on corruption"). 642 lib tests pass; all 5 fuzz targets re-run clean. |
| 2026-06-10 | (uncommitted) | Q-30 progress: pillar-C `shuttle` harness landed + **deterministic repro achieved** (fix still pending). Adopted `shuttle` for the BPM cone via a perf-preserving shim (`src/sync.rs`): prod stays on `parking_lot`, `--features shuttle` swaps in shuttle's instrumented primitives behind the same guard-returning API (thin wrappers absorb shuttle's `Result`-returning API). Converted the 4 BPM-cone files (`buffer_pool_manager`, `frame`, `page_guard`, `stats`) to `crate::sync`; replacers + `MemoryDiskManager` have no internal sync so needed no change. This partially reverses Q-25's blanket deferral — shuttle is adopted where it matters (the BPM) without the codebase-wide std migration Q-25 cited for perf. `tests/bpm_swap_shuttle.rs` (`#![cfg(feature = "shuttle")]`): two contending writers + a swapper over a tiny pool. **PCT found the race** (uniform-random needed 100k iters; that rarity is why the marquee is only ~25% flaky). Symptom: a writer holding `fetch_page_write(P2)` reads byte 0 == 0 — a *lost dirty write* (P2's dirtied page dropped, reloaded as the blank on-disk slot), same family as the marquee's cross-page corruption. Captured the exact failing schedule as a one-execution `ReplayScheduler` repro. All 3 shuttle race tests `#[ignore]`d pending the fix (they reproduce the open bug); default build/CI unaffected (642 lib + buffer tests pass, clippy 0). Next: root-cause the interleaving, fix the BPM, un-ignore + un-quarantine the marquee. |
| 2026-06-10 | (uncommitted) | **Q-30 fixed.** Root cause (confirmed via the deterministic `shuttle` replay + feature-gated trace): `evict_page` removed a dirty victim from the page table *before* flushing it (flush ran after `pt.write` was released), so a concurrent fetch of that page missed the cache and read the stale, un-flushed disk slot — a lost write. **Not** swap-specific; `swap_policy` just multiplied eviction churn (why the marquee with swaps failed ~25% and the no-swap bisection didn't). Fix: flush the victim while it stays **mapped + pinned** — Phase 1 selects + pins (reservation, so a concurrent evictor's pin check abandons rather than double-owning); Phase 2 flushes with `pt.write` released (I/O stays off the hot path) while the page is still mapped (concurrent fetches cache-hit correct data, never a stale slot); Phase 3 unpins, and if a racing fetch pinned/re-dirtied it, abandons (the racer re-tracked it), else removes it from the replacer (in case a read-hit re-tracked it) + the page table and hands off a clean frame. A naive first cut (flush-while-mapped without the pin reservation) traded the stale-read for a double-owner race — shuttle caught it in milliseconds, validating the harness. Verified: shuttle PCT (50k) + random (100k) explore clean; marquee 25/25 in release (was 3/12); `concurrency_stress`/`deadlock_stress`/buffer integration + 642 lib pass; clippy 0. Marquee un-quarantined; the obsolete one-shot replay test dropped (its schedule diverges post-fix — PCT is the regression of record). A zero-cost `sync_trace!` macro (`src/sync.rs`, prod no-op) was kept for future model-check debugging. |
| 2026-06-10 | (uncommitted) | **Q-30 reopened + fully fixed (second root cause).** The "Q-30 fixed" entry above closed only the *evict-side* race (flush-before-unmap); CI's `test (release)` failed again on the marquee with **cross-page** corruption (`byte 0 of PageId(0) == 174`, i.e. page 173's marker), a different symptom than the lost-write. Real root cause: **concurrent misses of the same page were never serialized.** `fetch_page_internal` drops the `page_table` read lock the instant it sees a miss, so two threads both reach `handle_cache_miss` for the same page, both allocate a frame, both read from disk, and both `pt.insert` — the loser's frame holds the page but is mapped nowhere: a *ghost*. Ghosts stay evictable, and `evict_page`'s unmap is keyed by the victim's `page_id`, so evicting a ghost deletes a **live** page-table entry / frees a frame the pool still treats as resident → an unrelated page's slot gets another page's bytes. Release-only because the miss→insert window only overlaps when threads truly run in parallel (debug is too slow); the swap storm amplifies it (each swap re-registers the replacer 6×, multiplying eviction churn). Fix (`handle_cache_miss`): re-validate under the publishing `pt.write` — if another thread already loaded the page, adopt it as a cache hit (pin under `pt.write` so a racing evictor abandons) and return our frame to the free list; disk I/O stays above the lock. Lock order unchanged (page_table outer, replacer inner). New regression test `concurrent_same_page_miss_no_ghost_frames` (tiny 3-frame pool + 16-thread barrier-synced herd over 24 pages) asserts frame conservation (`free + mapped == pool_size` at quiescence — a ghost breaks it) + content integrity: **5/5 fail pre-fix** (caught `free 0 + mapped 2 != pool 3`), **15/15 pass post-fix**. Verified: marquee 25/25 release (was ~3/12); full `policy_swap_stress_test` 10/10; 642 lib + buffer/concurrency integration pass; fmt + clippy `-D warnings` clean. The evict-side fix from the prior entry stays — both races were real and independent. |
| 2026-06-12 | (uncommitted) | Q-30 follow-up: closed the `shuttle` coverage gap that let the second root cause escape. The existing `bpm_swap_shuttle.rs` model (two writers + a swapper) explored clean even with the same-page-miss bug present, because its writers each touch the shared page only once — the scheduler is never free to land two threads in `handle_cache_miss` for the same page at once. Added a second, swap-free model `same_page_miss_scenario` (+ `shuttle_same_page_miss_{random,pct}`): two threads fetch one guaranteed-missing page concurrently, detecting the ghost via **frame conservation** (`free + mapped == POOL_SIZE` at rest). Proven a real guard, not decoration: checking out `buffer_pool_manager.rs` at `8f25803` (pre-second-fix), the 2 new tests **fail** (`free 0 + mapped 1 != pool 2`, with a deterministic replay seed) while the 2 original swap tests still **pass** — confirming the blind spot; at HEAD all 4 pass. fmt + clippy (incl. `--features shuttle`) clean. |
| 2026-06-12 | (uncommitted) | **Q-32 opened + first cut landed** (stability.md pillar D, leverage rank #3). Deterministic crash-at-every-LSN recovery torture test, `tests/dst_recovery_test.rs`. A seeded explicit-txn workload runs once to produce a **golden WAL**, then for *every* record boundary the WAL is truncated to that point, recovered into a **fresh in-memory engine** (recovery is WAL-only, so no engine-crash-image consistency problem), and the recovered state is checked against an **oracle** (a txn committed iff its Commit record is within the cut; committed writes applied in commit order). Uses explicit txns so the oracle reads only WAL *headers* (txn_id, is-commit) + record boundaries — never the MVCC-encoded payloads. Asserts (1) recovered state == oracle over the full key universe and (2) recovery idempotent, at every crash point. Smoke (1 seed, CI) + `#[ignore]` soak (16 seeds × larger workloads). Harness teeth verified: an injected oracle off-by-one (`ci <= cut`) fails at commit boundaries where **real recovery correctly returns `None`** for a txn whose Commit was truncated. Found no recovery bugs (good — corroborates the existing hand-written `crash_recovery_test` cases, now swept exhaustively). The doc's premise that `FaultInjectionDiskManager` already does torn writes / crash-at-LSN is **wrong** (it only schedules I/O errors) — corrected in `docs/stability.md`. **Remaining under Q-32 (next milestone):** extend the fault injector with torn page writes against the *engine* data file to exercise invariant "no torn page passes checksum undetected" (`Page::verify_checksum`), and multi-segment WAL truncation. |
| 2026-06-12 | (uncommitted) | **Q-32 milestone 2 landed: torn-page detection.** Extended `FaultInjectionDiskManager` with a one-shot `with_torn_next_node_write(keep)` — the next B-tree node page write persists only its first `keep` bytes (rest read back zero), modeling a write interrupted mid-page; non-node writes (header, raw data) pass through so the tear lands on a checksummed structure. New torture test `tests/torn_page_recovery_test.rs`: build a tree on a file-backed engine, tear the first node-page flush (`keep = PageHeader::SIZE` → valid header + new checksum, zeroed body), drop, reopen (no injection), and scan. The torn node must be **detected** — surfaced as a corruption `Err` or a decode-time panic — never silently accepted; a clean control (no tear) scans all keys, and a teeth-check (disarming the tear) confirms the forbidden `Ok(Ok(count))` arm fires ("SILENTLY ACCEPTED"). Finding: detection here came via the **structural** type check (`StorageCorrupted("Expected leaf … got internal")`), not the CRC assert — `keep=13` zeros the child pointers, so navigation trips before decode's `assert_eq!(… "checksum mismatch")`. Both are valid "crash on corruption" backstops; the test accepts either and only forbids silent acceptance. 2 unit tests for the injector (`torn_node_write_zeros_body_past_keep`, `torn_write_skips_non_node_pages`). fmt + clippy `-D warnings` clean; `fault_injection_test` (29) + `crash_recovery_test` (9) unaffected. **Remaining under Q-32:** a structurally-valid single-byte body corruption to exercise the CRC assert *specifically* (the structural check currently shadows it), and multi-segment WAL truncation. |
| 2026-06-14 | (uncommitted) | **Q-33 landed: conformance-matrix testing (stability.md pillar G).** Made the test suite exploit the source's interchangeability — write each swappable trait's contract once, run it across every implementation. New idiomatic `testkit` workspace crate (dev-dependency of root; source untouched, no cycle, `exclude=["fuzz"]`): `matrix.rs` holds x-macro registries (`for_each_policy!`/`for_each_disk!`/`for_each_engine!`) as the single source of truth — adding a config is one line and it flows into every test and bench. Per-axis ctors + universal contracts in `policy`/`disk`/`engine`; `workload` (seeded `Vec<Op>` data, replayable by tests/benches/future-shuttle); `equivalence` (`assert_all_equal` differential); `handles` (`Built<T>` keeps TempDirs alive); the fault injector moved here from `tests/common` (as a lib's public API it can never trip `dead_code` again, retiring the `mod common;` fragility). New suites: `replacer_conformance` (6 policies), `disk_manager_conformance` (3 backends incl. fault-passthrough proving transparency), `engine_conformance` (BTree+LSM), `config_equivalence` (3 differentials — same workload → identical state across all 6 policies / both DMs / both engines: the interchange thesis *asserted*). Folded `policy_swap_stress::fresh_policy` onto the registry (killed the parallel 6-policy list). Parametrized the Q-32 durability sweep over engines — **`Database<LsmEngine>` now proven through the full txn/WAL/MVCC/recovery stack** (previously only BTree exercised it; no LSM bug surfaced). Head-to-head `benches/config_matrix` consumes the *same* registry + workload (criterion comparison group per axis). All object-safe by design (the trait's `scan<R>` is fenced `where Self: Sized`), so registries are uniform runtime `Vec`s; the durability layer uses compile-time generics since `Database<E>` isn't dyn. Mechanism is a hand-rolled x-macro (no `rstest` dep). fmt + clippy `-D warnings` clean; new+touched suites all green. Payoff: the deferred fractal-tree engine inherits the entire suite + a bench slot from one registry line. **Kept** `cross_engine_differential` (a complementary *proptest* differential). **Remaining:** lift the registry to a place benches share without `#[path]` (already done via the crate); optional `for_each_engine` Database-maker helper; structurally-valid single-byte torn corruption. On branch `q33-testkit-matrix`. |
| 2026-06-14 | (uncommitted) | **Q-33 step 1: proptest × matrix.** The matrix gives two primitives — a config registry and workloads-as-`Vec<Op>`-data; each testing modality is an op-stream *generator* on that seam. Landed the proptest generator: `testkit::workload::op_strategy(max_len, key_space)` (a proptest `Strategy<Value = Vec<Op>>`) + `tests/config_proptest.rs` with registry-driven property-based differentials — `all_policies_agree` (6 policies, pool 4 ⇒ real eviction, 8 cases × long streams) and `all_engines_agree` (BTree+LSM, 48 cases × short streams). Upgrades the deterministic `config_equivalence` to *explore* the workload space: every config must agree on every generated stream. `cross_engine_differential` (Q-09) **kept** as the complementary *deeper* per-op engine check (get/scan after each op vs a `BTreeMap` oracle); reciprocal doc notes tie the two together. Added `proptest` to `testkit` deps. The remaining two generators (shuttle × workload, fuzz × matrix) are scoped with concrete entailment in `docs/stability.md` pillar G → "Future generators". Also fixed a stray missing `## Leverage ranking` header in that doc (collateral from the pillar-G insertion). fmt + clippy `-D warnings` clean; `config_proptest` 2 passed (7.7s), `cross_engine_differential` 3 passed. |
| 2026-06-15 | (uncommitted) | **Q-34: MVCC isolation behind a trait + the conformance matrix** (stability.md pillar E). Extracted the concurrency-control decisions that define an isolation level into `IsolationPolicy` (`src/txn/isolation/`): `read_view`, `visible`, `on_write` (per-write conflict check over pre-scanned versions — keeps the trait object-safe vs. the generic engine), `validate_commit`. `SnapshotIsolation` holds today's behavior; `ReadCommitted` is the second impl (fresh-per-read view, no first-committer-wins). Threaded `Arc<dyn IsolationPolicy>` through `TransactionManager` (default SI) → `TxnEngine` (get/scan build a visibility closure; put/delete call `check_write`) → `Database::commit_txn` (`validate_commit`); `mvcc_get`/`mvcc_scan` now take a visibility predicate closure (mechanism stays free of the policy). Added `Database::open_with_isolation`. Staged as a pure refactor (steps 1–4: SI byte-identical — `txn_isolation`/`mvcc_*`/`hermitage_si`/`acid`/`crash_recovery`/`dst` all green unchanged), then RC + matrix (step 5). **NOT an equivalence matrix** — isolation levels differ by design; the contract is per-level anomaly-spectrum conformance. `testkit::isolation` runs 4 Hermitage-style scenarios (g0 dirty-write, non-repeatable-read, lost-update P4, write-skew G2-item); `tests/isolation_conformance.rs` generates one `#[test]` per level from `for_each_isolation!`. **Teeth:** SI blocks non-repeatable-read + lost-update where RC allows them; both block dirty-write and allow write-skew — proven by both levels passing their *opposite* expected specs (2 passed, 5.7s). Unlike the policy/disk/engine axes, this axis isn't object-safe at the `Database<E>` layer, but the policy trait itself is `dyn`. NOTE (perf): `visible()` is now a vtable call per version on the read path — monomorphize via `TxnEngine<E,P>` later if it matters. fmt + clippy `-D warnings` clean. **Remaining:** SSI (needs `on_read` rw-edge tracking + real `validate_commit`); a fuller Hermitage set (G1a/b/c, OTV, PMP). On branch `q34-mvcc-isolation-trait`. |
| 2026-07-01 | (uncommitted) | **Q-35 CLOSED — root-caused and fixed via the prescribed shuttle discipline (measure→test→trace→fix); zero inspection-guessing.** **(1) Measure:** marquee 25/25, no-swap 20/20 in release — residual rate below stress-test detectability, confirming shuttle as the only viable detector. **(2) Model (the crux):** `three_writer_storm_scenario` composes the dimension both pairwise models lacked — a THIRD concurrent evictor — alongside dirty writers, same-page misses, guard drops, and policy swaps. PCT found a failing schedule in 0.16 s. **(3) Trace:** `sync_trace!` instrumentation over a `ReplayScheduler` seed gave the exact interleaving; temporary policy-level tracing in FIFO pinned the final contradiction. **(4) Root cause — a CLASS, not a site.** Replacer evictability is derived state (`pin_count == 0`) written by racing writers, each computing it at one instant and writing at another. Two schedules proven: **(a) Leak:** eviction phase-3 abandon dropped its reservation pin without restoring evictability; the racing fetch's unpin saw pin=1 (masked by the reservation) so its deferred `set_evictable(true)` never fired → mapped, unpinned, permanently un-evictable frame → NoFreeFrames with nothing pinned. **(b) Clobber — caught fix (a) racing:** reading the pin count BEFORE taking the replacer lock let a concurrent unpin's restore land first, then the stale write clobbered it — the same TOCTOU one layer down. **Fixes (3):** (i) abandon path re-tracks the victim (`record_access`) and restores evictability; (ii) CLASS RULE — every evictability write derives from a pin-count read taken INSIDE the replacer-lock hold (`unpin_page_internal` + abandon); all writers serialize on that lock so the last write carries the freshest truth in every order; (iii) CONTRACT `evictable ⊆ tracked` enforced in all six policies (was Clock-only; LRU scored untracked phantoms 0 = preferred victim — the strongest candidate mechanism for the historical cross-page corruption) and documented on `EvictionPolicy::set_evictable`. **Tooling:** `sync_trace!` now no-ops during panic unwind (its `current::me()` call during guard-drop unwinding collided with shuttle's schedule serialization and destroyed the diagnostics); BPM trace points kept (prod no-ops); `shuttle_q35_replay` harness documents that seeds go stale when sync ops change. **Verification:** buffer unit 99/99; shuttle hunts ×5 fresh-seed runs = 30/30 (3 models × 2 schedulers); marquee 25/25 release; no-swap 20/20; full suite 1323/0. The storm model stays as the permanent Q-35 regression guard. |
| 2026-06-18 | (uncommitted) | **Q-35 opened — Q-30 recurrence, third BPM eviction/swap interleaving. TOP PRIORITY (data corruption).** The marquee storm failed again: `byte 0 of PageId(12) == 23` — page index 22's marker in page 12's slot, the same **cross-page** corruption family as Q-30's second root cause. **Attribution (settled):** NOT a regression from the read-path/optimizer work in flight — `policy_swap_stress_test` drives only the `BufferPoolManager` (`fetch_page_*`, `new_page`, `swap_policy` over `MemoryDiskManager`); this session changed **zero** BPM/replacer/frame/disk code, so the exercised paths are byte-identical to HEAD. **Characterization:** rare — 15/15 marquee passes locally in release (vs ~3/12 at the Q-30 peak), so the two prior fixes (evict-side flush-before-unmap; same-page-miss re-validate-under-pt.write) hold for *their* interleavings; this is a residual *third* path. **The two `bpm_swap_shuttle.rs` models both pass** (`swap_vs_eviction` + `same_page_miss`, PCT + random, 27 s) → the failing interleaving is **outside the modeled space** — exactly the blind-spot pattern that let the 2nd root cause escape until `same_page_miss_scenario` was added. **HOW TO PROCEED — measure → trace → test → fix; never inspection-guess** (a naive guess already "traded the stale-read for a double-owner race" once; shuttle caught it in ms): **(1) MEASURE** — loop the marquee in release to quantify the residual rate; run the no-swap `isolation_concurrent_rw_without_swaps` bisection (historically 0/20) to confirm it's still swap-amplified, narrowing the cone to eviction-under-swap. **(2) TEST/REPRO (the crux)** — extend `tests/bpm_swap_shuttle.rs` with the missing dimension: the two models are read-mostly or single-shared-page; the marquee composes **concurrent writers (the dirty path) + a swapper + same-page contention simultaneously** — a 3-way race the pairwise models can't reach. Build that combined scenario (tiny pool, ≥2 dirty writers + a swapper hammering a hot sub-set) under the **PCT scheduler** (history: PCT found Q-30 in ~50 k iters where uniform-random needed 100 k). Invariants to assert: every fetch of P returns P's own marker (cross-page guard) + frame conservation `free + mapped == POOL` at rest (ghost guard). Capture the failing schedule as a one-shot `ReplayScheduler` seed. **(3) TRACE** — enable the zero-cost `sync_trace!` macro (`src/sync.rs`, prod no-op) under `--features shuttle` to print the exact lock/pin interleaving at the corruption point on the replayed seed. **(4) ROOT-CAUSE + FIX** under the deterministic replay; keep the new shuttle scenario as the permanent regression guard (the discipline that closed both prior root causes), and re-verify marquee ≥25/25 release. **CI hygiene while open:** the Q-30 precedent `#[ignore]`-quarantined the marquee (kept a `--ignored` repro + the no-swap bisection active) so per-push CI stays green/deterministic — apply the same if the flake disrupts the optimizer work (decision deferred to the user; not done unprompted, since it suppresses a corruption canary). |
